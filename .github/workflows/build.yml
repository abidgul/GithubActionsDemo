name: Build Workflow
on:
  workflow_dispatch:
  pull_request:
    branches:
      - development
jobs:
  build:
    runs-on: windows-2022
    steps:
    - name: Checkout
      uses: actions/checkout@v2
